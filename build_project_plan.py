from docx import Document
from docx.shared import Cm, Pt, RGBColor
from docx.oxml import OxmlElement
from docx.oxml.ns import qn
from pathlib import Path

OUT=Path('海外投资风险识别与智能分析项目规划.docx')
d=Document()
s=d.sections[0]; s.page_height=Cm(29.7); s.page_width=Cm(21)
s.top_margin=Cm(2); s.bottom_margin=Cm(2); s.left_margin=Cm(2.2); s.right_margin=Cm(2.2)
for name in ['Normal','Title','Heading 1','Heading 2']:
 st=d.styles[name]; st.font.name='微软雅黑'; st.font.color.rgb=RGBColor(0,0,0)
 st.element.rPr.rFonts.set(qn('w:eastAsia'),'微软雅黑')
 st.font.size=Pt({'Normal':10.5,'Title':22,'Heading 1':16,'Heading 2':12}[name])
 st.paragraph_format.space_after=Pt(7)
 st.paragraph_format.line_spacing=1.15
d.styles['Normal'].paragraph_format.widow_control=True
def p(t): d.add_paragraph(t)
def h(t): d.add_heading(t,2)
def page(t): d.add_page_break(); d.add_heading(t,1)
def table(head, rows, widths):
 t=d.add_table(rows=1, cols=len(head)); t.autofit=False
 for c,w in zip(t.columns,widths): c.width=Cm(w)
 for c,x in zip(t.rows[0].cells,head): c.text=x
 for row in rows:
  for c,x in zip(t.add_row().cells,row): c.text=x
 for i,r in enumerate(t.rows):
  for c,w in zip(r.cells,widths):
   c.width=Cm(w); c.vertical_alignment=1
   pr=c._tc.get_or_add_tcPr()
   sh=OxmlElement('w:shd'); sh.set(qn('w:fill'),'E8EEF4' if i==0 else 'FFFFFF'); pr.append(sh)
   borders=OxmlElement('w:tcBorders')
   for side in ['top','left','bottom','right']:
    e=OxmlElement('w:'+side); e.set(qn('w:val'),'single'); e.set(qn('w:sz'),'4'); e.set(qn('w:color'),'D9D9D9'); borders.append(e)
   pr.append(borders)
   margins=OxmlElement('w:tcMar')
   for side in ['top','left','bottom','right']:
    e=OxmlElement('w:'+side); e.set(qn('w:w'),'90'); e.set(qn('w:type'),'dxa'); margins.append(e)
   pr.append(margins)
   for para in c.paragraphs:
    para.paragraph_format.space_after=Pt(3); para.paragraph_format.space_before=Pt(3)
    for run in para.runs: run.font.size=Pt(9.5); run.bold=i==0
  nr=OxmlElement('w:cantSplit'); r._tr.get_or_add_trPr().append(nr)
 repeat=OxmlElement('w:tblHeader'); t.rows[0]._tr.get_or_add_trPr().append(repeat)
 d.add_paragraph().paragraph_format.space_after=Pt(0)

d.add_heading('海外投资风险识别与智能分析\n项目规划',0)
p('本地部署与大模型分阶段实施方案')
p('规划版本  V1.0    ｜    2026年9月9日')
h('一  建设目标与总体安排')
p('建设面向中国企业在印度尼西亚制造业投资场景的风险识别与智能分析平台。基于甲方提供或确认的政策法规、新闻和典型案例，完成数据导入、风险事件识别、分类分级、影响分析、应对建议及 Web 展示，形成可运行、可演示、可验证的业务闭环。')
p('业务系统按本地部署设计，前端、后端、数据库、文件存储和轻量知识库部署在指定自有服务器。前期通过大模型 API 验证业务流程和分析效果；后期根据甲方服务器资源、网络条件及实际测试结果，确定可部署的本地模型及推理方案。')
table(['阶段','实施目标','阶段出口'],[
 ['一期  API 验证版','约6周；完成本地业务系统和 API 分析闭环','完成典型案例验证，形成可演示版本'],
 ['二期  本地模型适配','条件具备后约2–3周；硬件评估、部署与复测','确认本地模型满足约定效果和性能要求'],
 ['后续扩展','按正式使用需求另行规划','生产加固、多场景或自动采集等独立立项']
],[2.8,7.2,6.6])
h('二  范围与部署口径')
p('一期保持需求报告中的单国家、单行业、技术验证平台范围。本地部署属于本项目技术路线；API 验证期仍依赖外部模型服务，不等同于完全离线运行。后续本地模型适配纳入总体路线，具体工作量在服务器盘点后确认。')
p('当前不包含服务器或 GPU 采购、生产级高可用与容灾、复杂权限体系、自动互联网采集、实时监测、多国家多行业、知识图谱或 GraphRAG、风险概率预测、完整多语言系统及正式报告系统。生产环境加固需单独明确范围。')
p('排期为规划估算，以样本资料、风险分类规则、API 条件和开发人员到位为起点，不构成固定工期或报价承诺。')

page('三  功能建设与处理流程')
table(['模块','本阶段工作','可核验结果'],[
 ['数据导入','Excel、CSV、Word、PDF 及手工录入；字段映射、必填校验、重复提示和失败记录','原文及来源保留；失败原因可查看'],
 ['风险识别','识别风险事件；区分非风险信息及证据不足信息；支持单条或批量任务','形成风险记录与处理状态'],
 ['分类分级','加载甲方确认的分类体系和分级规则；红色高风险、橙色中风险、黄色一般关注','输出类型、等级及判断依据'],
 ['知识库辅助分析','资料解析、分段、索引及检索；关联政策法规和历史案例','检索证据可回查原文位置'],
 ['分析与建议','生成标题、摘要、潜在影响、风险依据、初步应对建议及来源','结果结构统一；依据与推断分开'],
 ['Web 展示','风险总览、列表、详情；按时间、类型和等级查询筛选；导入与任务状态页面','统计与列表一致；详情可追溯'],
 ['基础运维','基础登录保护、配置管理、任务日志、失败重试和备份恢复说明','服务可启动、故障可定位']
],[2.6,8.3,5.7])
h('处理流程')
p('资料导入 → 解析与校验 → 保存原文及来源 → 风险事件识别 → 检索相关知识 → 按规则分类分级 → 生成分析和建议 → 校验结构及引用 → 保存结果 → Web 展示与业务复核。非风险内容保留识别记录，证据不足内容标记为待复核。')
h('数据与证据管理')
p('每条记录保留标题、时间、来源、正文、原始链接和内部资料编号。知识库记录文件版本、章节或页码等可用位置；没有页码的资料使用段落编号。对法规记录适用地区及有效时间，过期或版本冲突资料需提示业务复核。')
p('同一资料可以包含多条事件，应分别关联原始资料；重复导入提示或去重。分析结果记录模型、提示词、分类规则与知识库版本，重新分析生成新版本，避免覆盖原有可追溯记录。')
p('一期优先支持可提取文本的文件。扫描件、复杂表格和加密文件在样本检查时确定处理方式；需要 OCR 时单独确认适配范围。外语资料纳入样本评测，不默认具备完整多语言能力。')

page('四  本地架构与大模型接入')
h('本地业务系统')
p('内网浏览器访问 Web 前端，后端统一承担导入、查询、分析任务和权限校验。数据库保存业务记录，文件存储保留原始资料，检索索引支撑轻量 RAG。批量分析采用后台任务，前端查看进度，避免长任务阻塞页面请求。')
p('部署优先保持组件精简，根据甲方操作系统、容器支持和运维要求确定具体技术栈。将业务服务与模型推理服务分开配置，使用稳定的输入输出结构连接，避免业务页面直接绑定某个模型厂商。')
h('一期调用大模型 API')
p('统一模型接入层管理服务地址、模型标识、凭据、超时、限流、重试和调用用量。凭据只保存在服务端。单次请求原则上发送待分析文本、必要的检索片段和分类规则，不上传整库。外部 API 调用失败时保留任务并支持重试，不生成伪成功结果。')
p('API 测试使用甲方确认可外发的资料或脱敏样本，事先明确服务提供方、数据范围和外网出口。资料不得外发时，先用可外发样本验证；实际敏感资料留待本地模型条件具备后测试。脱敏不能自动等同于获得外发授权。')
h('本地模型替换设计')
p('接入层分别适配外部 API 和本地推理接口，对上层提供一致的分析协议。模型切换时验证结构化输出、上下文长度、分类分级、引用完整性、处理时延和并发承载，不把更换接口地址视为迁移完成。')
p('知识库向量化与重排也应单独配置。优先在本地处理检索数据；如前期使用外部向量化或重排服务，同样纳入数据出网清单。更换向量模型可能需要重建索引，必须计入迁移和回归工作。')
h('离线运行与基本运维')
p('全部推理能力本地化后，检查模型、向量化、重排、OCR、前端资源、字体及安装依赖是否仍需要外网，并在禁止外网的环境执行核心流程测试。外部原始链接可以保留，但应能通过本地保存的原文完成依据核验。')
p('交付服务配置示例、启动顺序、健康检查、日志位置和备份恢复操作。测试环境与正式数据隔离；日志不记录 API 密钥，并控制原文和敏感信息的输出范围。本阶段提供基本保护，不扩大为企业级安全体系建设。')

page('五  实施排期与里程碑')
p('建议一期按6周组织，假设至少配置1名前后端开发和1名 AI／数据开发，项目管理与测试由兼任人员参与，甲方业务人员每周集中反馈。若由单人实施，或存在复杂 OCR、资料清洗、内网审批等情况，应重新估算排期。')
table(['时间','主要任务','里程碑与交付'],[
 ['第1周','梳理字段、分类与分级规则；检查资料格式；确认 API 出网边界；盘点甲方服务器','M1 范围基线、数据模板、评测样本及环境清单'],
 ['第2周','搭建本地前后端和存储；完成导入、解析及模型接入；打通单条分析','M2 单条数据从导入到结果展示可运行'],
 ['第3周','建设轻量知识库；完善风险识别、分类分级、引用与结构校验','核心 AI 流程和可追溯依据可验证'],
 ['第4周','完成总览、列表、详情；联调批量任务、筛选、重试及版本记录','M3 API 版本功能闭环完成'],
 ['第5周','开展功能测试与典型案例评测；修正规则、提示词及检索问题','评测记录、缺陷清单与优化版本'],
 ['第6周','部署目标本地环境；开展甲方业务验证、演示和交接','M4 API 版验收材料及部署交付包'],
 ['后续2–3周','服务器就绪后筛选并部署本地模型；复测效果、性能与离线依赖','M5 本地模型适配报告及条件满足后的切换版本']
],[2.3,8.1,6.2])
h('依赖关系与推进机制')
p('分类规则与样本确认影响分析开发和验收；外部 API 可访问且资料可外发是一期验证前提；本地模型阶段依赖服务器访问权限、算力资源和环境准备。服务器盘点从第1周启动，硬件采购、到货及审批等待时间不计入模型适配工期。')
p('每周安排一次演示和问题确认，以功能清单、案例结果和缺陷状态判断进度。新增国家、行业、数据来源或生产级要求进入变更清单，评估工期和费用后调整计划。')
h('阶段决策点')
p('M1 确认做什么和如何验证；M3 检查核心闭环是否可用；M4 固化 API 基线并完成一期交付；M5 比较本地模型与基线。若本地模型未达约定指标，形成差距及后续方案，不以“成功启动模型”替代业务验收。')

page('六  甲方服务器评估与模型选型')
p('本地模型暂不预定具体名称、参数规模或显卡配置。先盘点服务器，再使用项目实际数据选择候选模型；以效果、显存占用、吞吐、延迟和运维可行性共同决定部署方案。')
table(['需提供信息','具体内容','用于判断'],[
 ['计算资源','CPU 型号及核数、内存、GPU 型号与数量、每卡显存、可用余量','可运行模型范围及并发余量'],
 ['系统环境','操作系统及版本、处理器架构、GPU 驱动、容器权限、安装权限','推理运行环境与适配工作量'],
 ['存储与网络','可用磁盘、存储性能、外网出口、内网访问、端口及代理限制','模型和索引存储、交付及服务接入'],
 ['业务负载','资料量、每日新增量、文本长度、同时使用人数、期望响应时间','上下文长度、任务并发与容量目标'],
 ['管理要求','数据等级、完全离线要求、模型许可要求、备份与运维责任人','部署边界与可持续运维']
],[2.8,8.2,5.6])
h('选型与适配步骤')
p('第一步，依据硬件和许可条件筛选少量候选模型，确认模型文件、运行依赖和接口可在目标服务器使用。第二步，在相同评测集和知识库版本下比较 API 基线与本地候选，重点检查中文表达、实际资料语言、分类遵循、结构输出及证据引用。')
p('第三步，测量代表性短文本和长文本的峰值资源占用、处理时延与任务完成率，验证约定并发下的稳定性。第四步，在资源约束下调整上下文、批量大小和量化方式，并复测质量。最终提交模型、运行配置、适用负载和剩余问题说明。')
h('资源不足时的处理')
p('无可用 GPU 或显存紧张时，可以评估更轻量模型和串行批处理，但不提前承诺交互速度。若效果或速度达不到要求，选择缩减负载、增配资源或继续使用经甲方允许的 API 方案；若要求完全离线且本地条件不足，则先完成其他交付并明确模型部署待满足条件。')
h('切换与回退')
p('切换前备份数据库、索引与配置，保留 API 基线及评测记录。本地模型通过测试后修改接入配置，执行冒烟测试和小批量观察。回退到 API 仅在数据出网许可仍有效时使用；严格离线环境出现故障时暂停或排队分析，不自动将数据转发外部服务。')

page('七  验收与交付安排')
h('验收方法')
p('功能验收检查完整业务流程及异常处理，效果验收由甲方业务人员对典型案例复核。建议第1周共同建立30–50条评测案例，覆盖明显风险、非风险、边界案例、资料不足、不同风险等级及实际使用语言；保留一部分不参与调优的案例用于最终验证。该数量为规划建议。')
table(['验收项','检查内容与通过依据'],[
 ['功能闭环','约定格式导入后，完成识别、分类分级、分析建议及页面查询；非风险和失败任务有明确状态'],
 ['分类与等级','以甲方标注为参照，统计风险识别准确率与召回率、分类一致率、等级一致率；第1周确定目标值'],
 ['分析质量','甲方逐条评价摘要忠实度、影响合理性及建议参考价值；记录通过项与不通过原因'],
 ['来源追溯','结果中的引用可定位原始资料；不允许伪造来源；抽查所引片段是否支持对应判断'],
 ['异常与稳定性','重复文件、无效文件、超时、断网及任务重试可处理；核心流程无阻断性缺陷'],
 ['本地模型专项','使用相同测试集对比 API 基线；达到约定质量和时延指标；要求离线时完成断外网验证']
],[3.2,13.4])
p('质量目标和性能阈值需在样本及服务器条件明确后形成书面验收基线。业务上严重误判、依据缺失或不可追溯的问题须修复或由甲方明确接受；未达到指标的条目保留问题记录，不能只以平均分掩盖。')
h('交付清单')
p('一期交付：可运行的本地系统、前后端及 AI 模块源代码与依赖清单、部署脚本和配置模板、数据导入模板、轻量知识库、分类规则与提示词配置、评测结果、部署及使用说明、已知限制清单，并完成一次操作与维护交接。源代码交付方式及第三方许可边界在启动时确认。')
p('二期交付：服务器评估与模型选型报告、本地推理配置和安装说明、模型获取及许可说明、对比评测和性能记录、离线依赖清单、切换回退说明及适配后的系统版本。模型权重的交付方式遵循相应许可及甲方环境要求。')
h('职责与费用边界')
p('甲方负责数据和规则确认、资料外发范围确认、服务器及访问条件、业务评测和验收；乙方负责设计开发、数据解析、知识库、模型接入、部署、测试与交接。API 账户和调用费用承担方在启动时确认；硬件采购、额外数据授权、生产加固及新增功能费用单列。')
p('启动时优先落实四项输入：代表性资料、风险分类分级规则、API 测试条件、甲方服务器配置。先形成可验证的 API 基线，再用真实硬件测试决定本地模型，确保技术路线与业务效果同步落地。')

d.core_properties.title='海外投资风险识别与智能分析项目规划'
d.core_properties.subject='本地部署与大模型分阶段实施方案'
for st in d.styles:
 for border in list(st.element.iter(qn('w:pBdr'))): border.getparent().remove(border)
paras=list(d.paragraphs)+[p for t in d.tables for r in t.rows for c in r.cells for p in c.paragraphs]
for para in paras:
 pr=para._p.get_or_add_pPr()
 for border in list(pr.findall(qn('w:pBdr'))): pr.remove(border)
 snap=OxmlElement('w:snapToGrid'); snap.set(qn('w:val'),'0'); pr.append(snap)
 para.paragraph_format.line_spacing=Pt(16 if para.style.name=='Normal' else 23)
 if para.style.name=='Title': para.paragraph_format.line_spacing=Pt(30)
for t in d.tables:
 for r in t.rows:
  for c in r.cells:
   for para in c.paragraphs: para.paragraph_format.line_spacing=Pt(14)
d.save(OUT)
print(OUT.resolve())
